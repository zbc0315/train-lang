{
  "name": "@train-lang/cli",
  "version": "0.0.0",
  "description": "train language CLI — run/check/version/help",
  "license": "MIT",
  "type": "module",
  "main": "./dist/main.js",
  "types": "./dist/main.d.ts",
  "bin": {
    "train": "./dist/bin.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@train-lang/core": "workspace:*",
    "@train-lang/adapter-spec": "workspace:*",
    "@train-lang/adapter-mock": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "execa": "^9.5.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
