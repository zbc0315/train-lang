{
  "name": "train-lang",
  "version": "0.0.0",
  "private": true,
  "description": "train — a domain-specific language for LLM-driven workflows",
  "homepage": "https://github.com/train-lang/train",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "lint:error-codes": "node --experimental-strip-types scripts/check-error-codes.ts"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.0.0"
}
